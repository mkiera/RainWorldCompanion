namespace RainWorldCompanion;

/// <summary>
/// The display name only, spaced. Everything that identifies rather than describes spells it
/// RainWorldCompanion with no space: the assembly, the namespaces, the settings folder and the
/// mutex. The installer script and Directory.Build.props hold their own copies of this string.
/// </summary>
internal static class AppInfo
{
    public const string DisplayName = "RainWorld Companion";
}
